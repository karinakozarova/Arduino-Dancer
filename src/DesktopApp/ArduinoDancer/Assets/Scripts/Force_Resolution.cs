using UnityEngine;
using System.Collections;

public class Force_Resolution : MonoBehaviour {

	// Update is called once per frame
	void Update () {
        Debug.Log("forces resolution");
      //  Screen.SetResolution(1600, 800, false);
	}
}
